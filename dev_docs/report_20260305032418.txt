======================================================================
 開発進捗報告 (2026-03-05 03:24:18)
======================================================================

【やったこと・できたこと】
1. スカラー値による動的サイズ計算機構の実装
   - `src/config/mindmapConfig.ts` に基準の高さ (`BASE_NODE_HEIGHT = 60`) および形状ごとの縦横比 (`SHAPE_ASPECT_RATIOS`) を定義。
   - `src/utils/nodeSizeUtils.ts` にスカラー値（size）と形状（shape）から `width` と `height` を算出する共通ユーティリティ関数 `calculateNodeDimensions` を実装。
   - Dagre による自動レイアウト計算 (`layout.ts`) および中央配置処理において、この計算関数を経由して正確な寸法を参照するように改修。

2. スタイル変更モーダルの拡張 (`StyleNodeModal.tsx`)
   - モーダル内にノードの「形状（角丸・四角・円・カプセル・ひし形）」および「サイズ（小・中・大）」を選択できるプリセットUIを追加。
   - 複数選択されたノードに対して、カラーだけでなく形状とサイズ（スカラー値）も一括で更新・永続化できるようにストア連携を強化。

3. カスタムノードの表示改善 (`MindMapNode.tsx`)
   - ノードのコンテナに Flexbox (`flex flex-col justify-center items-center text-center`) を適用し、文字を上下左右中央揃えに配置。
   - 形状クラス動的適用ユーティリティ (`getShapeClassName`) を導入し、モーダルで変更した形状やサイズをリアルタイムに視覚反映。

【できなかったこと】
- 特になし（予定していた動的サイズ・形状変更およびモーダル・表示の統合が完了）

【とりあえずで仮置きしたが再検討が必要なこと】
- ひし形 (`diamond`) などの特殊形状におけるテキストの折り返しやパディングの微調整（現状の矩形ベースのFlexbox配置で概ね機能しているが、視認性の詳細なブラッシュアップは追って検討）

【できたが条件があること】
- スカラー値によるサイズ計算は、基準高さ `60px` に対する倍率（小: 0.8 / 中: 1.0 / 大: 1.4）として機能しており、形状変更時のアスペクト比と連動しているため、対応する `NodeShape` の定義が正しく一致している必要がある。

【つぎやるべきこと】
- ドラッグ移動時の Undo 履歴保存タイミングの最適化 (`onNodeDragStop` への変更)
- 自動レイアウト時の循環参照（ループ）検出・警告処理の検討